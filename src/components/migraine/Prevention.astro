---
import MigraineCard from './migraineCard/MigraineCard.astro'
---

<section class="container">
    <div class="section_title_container">
        <div class="section_title">
            <h3>PREVENTION</h3>
        </div>
    </div>
    <div class="prevention_row">
        {[1,2,3,4,5,6,7,8].map((item) => (
            <MigraineCard type='large' />
        ))}
      </div>
</section>

<style>
    .prevention_row{
    margin-top: 4rem;
        }
    @media screen and (min-width: 990px){
        .prevention_row{
            max-width: 75rem;
            /* padding: 0;
            display: grid;
    margin: 0;
    list-style: none;
    grid-template-columns: repeat(2,calc(50% - 3rem));
    grid-column-gap: 6rem; */
    margin-top: 4rem;
        }
    }
</style>